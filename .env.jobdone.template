# JobDone PeerDB Configuration
# External PostgreSQL and Security Settings

# ==============================================================================
# EXTERNAL POSTGRESQL - Your existing PostgreSQL
# ==============================================================================
# PeerDB Catalog Database
PEERDB_CATALOG_HOST=your-postgres-host.example.com
PEERDB_CATALOG_PORT=5432
PEERDB_CATALOG_USER=peerdb_user
PEERDB_CATALOG_PASSWORD=your-secure-postgres-password
PEERDB_CATALOG_DATABASE=peerdb

# Temporal Databases (if using same PostgreSQL server)
# Set these to use pre-created databases for Temporal
# If not set, Temporal will try to create its own databases
TEMPORAL_DB_HOST=${PEERDB_CATALOG_HOST}
TEMPORAL_DB_PORT=${PEERDB_CATALOG_PORT}
TEMPORAL_DB_USER=${PEERDB_CATALOG_USER}
TEMPORAL_DB_PASSWORD=${PEERDB_CATALOG_PASSWORD}
TEMPORAL_DATABASE=temporal
TEMPORAL_VISIBILITY_DATABASE=temporal_visibility

# ==============================================================================
# SECURITY - Change ALL these passwords!
# ==============================================================================

# PeerDB Server Password (for SQL interface on port 9900)
PEERDB_PASSWORD=jobdone-peerdb-password-change-me

# NextAuth Secret for session encryption (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=generate-random-32-char-string-change-me

# MinIO Admin Credentials (S3-compatible storage)
MINIO_ROOT_USER=jobdone-minio-admin
MINIO_ROOT_PASSWORD=jobdone-minio-password-change-me

# ==============================================================================
# OPTIONAL: AWS S3 / Google Cloud Storage (leave empty to use local MinIO)
# ==============================================================================
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_ENDPOINT=

# ==============================================================================
# OPTIONAL: Restrictions
# ==============================================================================
# Comma-separated list of allowed target databases (empty = all)
# PEERDB_ALLOWED_TARGETS=postgres,snowflake,bigquery

# Comma-separated list of allowed ClickHouse domains (empty = all)
# PEERDB_CLICKHOUSE_ALLOWED_DOMAINS=*.clickhouse.cloud

# ==============================================================================
# OPTIONAL: Features
# ==============================================================================
PEERDB_EXPERIMENTAL_ENABLE_SCRIPTING=true